<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rpm.mapper.update.DeleteCRMapper">
	<delete id="deleteComp">
		delete from complaint_t where complaint_id=#{complaint_id}
	</delete>
	
	<delete id="deleteRepair">
		delete from repair_t where repair_id=#{repair_id}
	</delete>
	
</mapper>